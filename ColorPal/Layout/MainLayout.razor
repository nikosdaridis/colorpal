@inherits LayoutComponentBase

@inject NavigationManager NavigationManager

<main class="min-w-[350px] max-w-[350px]">
    @Body
</main>

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
            NavigationManager.NavigateTo("/");
    }
}
